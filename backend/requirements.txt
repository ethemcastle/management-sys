# Pinned-ish runtime deps (mirror of pyproject.toml [project.dependencies]).
# For a modern workflow prefer `uv sync` or `pip install -e .`; this file is
# provided for plain `pip install -r requirements.txt` environments.
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0.36
pydantic>=2.9
pydantic-settings>=2.6
alembic>=1.14
httpx>=0.27
