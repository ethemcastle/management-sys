export const environment = {
  production: false,
  // Relative path — the dev server proxies `/api` to the backend (proxy.conf.js),
  // so requests are same-origin (no CORS, no localhost IPv4/IPv6 ambiguity).
  apiBase: '/api',
};
